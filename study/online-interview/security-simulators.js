(function(){
'use strict';let active=null;
const H=(k,t,d)=>`<div class="concept-head"><div><span>${k}</span><h3>${t}</h3><p>${d}</p></div><button data-sec-action="close">닫기</button></div>`,S=(l,m)=>`<div class="concept-status" aria-live="polite"><b data-sec-label>${l}</b><span data-sec-message>${m}</span></div>`;
const host=()=>active?document.querySelector(`[data-flow-host="${active.id}"]`):null,q=s=>host()?.querySelector(s)||null,qa=s=>host()?[...host().querySelectorAll(s)]:[],tx=(s,v)=>{const n=q(s);if(n)n.textContent=v},say=(l,m)=>{tx('[data-sec-label]',l);tx('[data-sec-message]',m)};
function m81(){return `<section class="concept-demo">${H('Q81 · reachable movement envelope','패킷 속도 값이 정상이어도 벽을 건너뛴 이동은 가능할까?','경과 시간·이동 거리를 바꿔 서버 규칙상 도달 가능한 영역과 실제 경로를 비교하세요.')}<div class="concept-controls"><label>경과 시간 <input type="range" min="100" max="1500" step="100" value="500" data-speed-time><output data-speed-time-v>500ms</output></label><label>요청 거리 <input type="range" min="2" max="20" value="6" data-speed-distance><output data-speed-distance-v>6m</output></label></div><div class="mm-arena"><div class="speed-course"><div class="speed-start">START</div><div class="speed-envelope" data-speed-envelope></div><div class="speed-wall"></div><i class="speed-path" data-speed-path></i><div class="speed-end" data-speed-end>END</div></div></div>${S('도달 가능성 계산','서버는 속도 상한, 경과 시간, 충돌 경로, 상태 효과를 함께 검증합니다.')}</section>`}
function speed81(){const t=+q('[data-speed-time]').value,d=+q('[data-speed-distance]').value,max=t/1000*8,blocked=d>10,valid=d<=max&&!blocked;tx('[data-speed-time-v]',`${t}ms`);tx('[data-speed-distance-v]',`${d}m`);q('[data-speed-envelope]').style.setProperty('--reach',`${Math.min(80,max*4)}%`);q('[data-speed-end]').style.setProperty('--x',`${8+d*4}%`);q('[data-speed-path]').classList.toggle('is-invalid',!valid);say(valid?'가능한 이동':'서버 규칙 위반',blocked?'직선 속도는 정상이어도 권위 충돌 경로가 벽을 통과합니다.':valid?'시간·속도·경로 envelope 안에 있습니다.':`최대 ${max.toFixed(1)}m인데 ${d}m를 요청했습니다.`)}
function m82(){return `<section class="concept-demo">${H('Q82 · 관찰 기반 rewind window','클라이언트가 과거 시간을 마음대로 고르면 얼마나 되감을 수 있을까?','서버가 관찰한 RTT 범위와 클라이언트 주장 시각을 바꿔 인정 범위를 확인하세요.')}<div class="concept-controls"><label>관찰 RTT <input type="range" min="30" max="350" value="120" data-rtt><output data-rtt-v>120ms</output></label><label>과거 주장 <input type="range" min="0" max="600" value="90" data-claim><output data-claim-v>90ms</output></label></div><div class="mm-arena"><div class="rewind-window"><div class="rewind-axis"><div class="rewind-valid" data-rewind-valid></div><i class="rewind-mark" style="--x:100%"></i><div class="rewind-claim" data-rewind-claim>claim</div></div><div class="rewind-facts"><span>과거 ←</span><span data-rewind-limit>허용 0ms</span><span>server now</span></div></div></div>${S('서버 관찰치로 상한 설정','클라이언트 timestamp는 서버 수신 시각과 RTT·jitter로 만든 bounded window 안에서만 사용합니다.')}</section>`}
function rewind82(){const r=+q('[data-rtt]').value,c=+q('[data-claim]').value,limit=Math.min(250,Math.round(r*.65+40)),ok=c<=limit;tx('[data-rtt-v]',`${r}ms`);tx('[data-claim-v]',`${c}ms`);tx('[data-rewind-limit]',`허용 ${limit}ms`);q('[data-rewind-valid]').style.setProperty('--left',`${100-limit/6}%`);q('[data-rewind-claim]').style.setProperty('--x',`${100-c/6}%`);q('[data-rewind-claim]').classList.toggle('is-rejected',!ok);say(ok?'bounded rewind 허용':'시간 조작 거절',ok?'서버 관찰 범위 안의 과거 snapshot으로 판정합니다.':`주장 ${c}ms가 관찰 기반 상한 ${limit}ms를 넘었습니다.`)}
function m83(){return `<section class="concept-demo">${H('Q83 · 표시와 권위 분리','메모리에서 골드를 99만으로 바꾸면 실제 구매도 성공할까?','클라이언트 표시를 변조한 뒤 서버 잔액 기반 구매 transaction을 실행하세요.')}<div class="mm-arena"><div class="wallet-lab"><div class="wallet-view"><h4>CLIENT UI</h4><div class="wallet-value" data-client-wallet>1,000G</div><button data-sec-action="tamper">메모리 999,999G로 변경</button></div><div class="wallet-gate">→</div><div class="wallet-view"><h4>SERVER LEDGER</h4><div class="wallet-value" data-server-wallet>1,000G</div><div>ITEM price 2,000G</div></div><div class="wallet-receipt" data-wallet-receipt>no purchase</div></div></div><div class="concept-controls"><button class="primary" data-sec-action="purchase">2,000G 구매 요청</button><button data-sec-action="reset">초기화</button></div>${S('표시는 캐시','가격·잔액·지급 결과는 서버 transaction이 결정합니다.')}</section>`}
function tamper83(){tx('[data-client-wallet]','999,999G');q('[data-client-wallet]').classList.add('is-tampered');say('로컬 표시만 변조','서버 ledger 1,000G와 어떤 권위 상태도 바뀌지 않았습니다.')}
function purchase83(){tx('[data-wallet-receipt]','P-83 · REJECT insufficient_funds · authoritative balance 1,000G');say('구매 거절','클라이언트가 보낸 잔액을 읽지 않고 서버 원장에서 가격과 보유액을 원자적으로 확인합니다.')}
function m84(){return `<section class="concept-demo">${H('Q84 · nonce·sequence replay 차단','암호화된 구매 패킷을 그대로 다시 보내면 왜 막아야 할까?','동일 nonce와 sequence의 캡처 패킷을 반복 전송해 replay window 동작을 확인하세요.')}<div class="mm-arena"><div class="replay-stream" data-replay-stream><div class="replay-row"><b>SEQ 441</b><span>nonce N-9 · BUY P-84</span><span>대기</span></div></div><div class="replay-window" data-replay-window>accepted sequence window · 438~445</div></div><div class="concept-controls"><button class="primary" data-sec-action="send84">원본 전송</button><button data-sec-action="replay84">캡처 패킷 3회 replay</button><button data-sec-action="reset">초기화</button></div>${S('아직 미사용 nonce','서명은 변조을 막고 nonce·sequence는 같은 의도의 재사용을 막습니다.')}</section>`}
function send84(){if(active.seen){say('duplicate 결과 replay','SEQ 441의 기존 receipt만 반환합니다.');return}active.seen=true;q('[data-replay-stream]').innerHTML='<div class="replay-row is-accepted"><b>SEQ 441</b><span>N-9 · BUY P-84</span><span>ACCEPT</span></div>';say('첫 요청 승인','nonce N-9를 소비하고 P-84 효과를 한 번 commit했습니다.')}
function replay84(){if(!active.seen)send84();q('[data-replay-stream]').innerHTML+=Array.from({length:3},(_,i)=>`<div class="replay-row is-rejected"><b>REPLAY ${i+1}</b><span>SEQ 441 · nonce N-9</span><span>DROP</span></div>`).join('');say('3회 replay 차단','복호화와 서명이 유효해도 이미 소비된 sequence·nonce라 새 transaction을 만들지 않습니다.')}
function heat(pattern){return Array.from({length:96},(_,i)=>{const v=pattern==='bot'?((i%24>2&&i%24<20)?(.65+(i%3)*.12):.08):(.12+((i*37)%80)/100);return `<i class="bot-cell" style="--v:${Math.min(.95,v).toFixed(2)}"></i>`}).join('')}
function m85(){return `<section class="concept-demo">${H('Q85 · 장기 행동 일관성','간격에 노이즈를 넣은 봇을 한 번의 입력으로 구분할 수 있을까?','관찰 기간과 행동 유형을 바꿔 시간대·경로·반응의 장기 패턴을 누적하세요.')}<div class="concept-controls"><label>행동 유형 <select data-behavior><option value="human">사람</option><option value="bot">노이즈 봇</option></select></label><label>관찰 일수 <input type="range" min="1" max="14" value="2" data-days><output data-days-v>2일</output></label></div><div class="mm-arena"><div class="bot-heat" data-bot-heat></div><div class="bot-signals"><div class="bot-signal" data-bot-signal="0">24시간 주기</div><div class="bot-signal" data-bot-signal="1">경로 반복</div><div class="bot-signal" data-bot-signal="2">반응 분산</div></div><div class="bot-score" data-bot-score>증거 부족</div></div>${S('표본 수집','개별 입력 간격보다 긴 시간의 휴식, 목표 선택, 실패 후 적응을 함께 봅니다.')}</section>`}
function behavior85(){const bot=q('[data-behavior]').value==='bot',days=+q('[data-days]').value,signals=bot?Math.min(3,Math.floor(days/3)):0;q('[data-bot-heat]').innerHTML=heat(bot?'bot':'human');qa('[data-bot-signal]').forEach((n,i)=>n.classList.toggle('is-on',i<signals));tx('[data-days-v]',`${days}일`);tx('[data-bot-score]',signals<2?'증거 부족':signals===2?'검토 대상':'높은 자동화 가능성');say(signals<2?'관찰 계속':signals===2?'복합 패턴 발견':'장기 일관성 확인',signals<2?'짧은 표본으로 자동 제재하지 않습니다.':'노이즈가 있어도 휴식·경로·반응의 결합 패턴이 반복됩니다.')}
const M={81:m81,82:m82,83:m83,84:m84,85:m85};
function open(id){close(false);document.dispatchEvent(new CustomEvent('interview-demo-open',{detail:{id}}));active={id,seen:false};const h=document.querySelector(`[data-flow-host="${id}"]`),b=document.querySelector(`[data-flow-open="${id}"]`);if(b){b.hidden=true;b.setAttribute('aria-expanded','true')}h.innerHTML=M[id]();if(id===81)speed81();if(id===82)rewind82();if(id===85)behavior85()}
function reset(){const id=active.id;active={id,seen:false};host().innerHTML=M[id]();if(id===81)speed81();if(id===82)rewind82();if(id===85)behavior85()}
function close(f=true){if(!active)return;const id=active.id,h=document.querySelector(`[data-flow-host="${id}"]`),b=document.querySelector(`[data-flow-open="${id}"]`);if(h)h.innerHTML='';if(b){b.hidden=false;b.setAttribute('aria-expanded','false');if(f)b.focus({preventScroll:true})}active=null}
function init(){document.addEventListener('click',e=>{const o=e.target.closest('[data-flow-open]');if(o){const id=+o.dataset.flowOpen;if(id>=81&&id<=85){open(id);return}}const b=e.target.closest('[data-sec-action]');if(!b||!active)return;const x=b.dataset.secAction;if(x==='close')close();if(x==='reset')reset();if(x==='tamper')tamper83();if(x==='purchase')purchase83();if(x==='send84')send84();if(x==='replay84')replay84()});document.addEventListener('input',e=>{if(e.target.matches('[data-speed-time],[data-speed-distance]'))speed81();if(e.target.matches('[data-rtt],[data-claim]'))rewind82();if(e.target.matches('[data-behavior],[data-days]'))behavior85()});document.addEventListener('change',e=>{if(e.target.matches('[data-behavior]'))behavior85()});document.addEventListener('interview-demo-open',e=>{if(active&&active.id!==e.detail.id)close(false)})}
window.SecuritySimulators={init};
})();
