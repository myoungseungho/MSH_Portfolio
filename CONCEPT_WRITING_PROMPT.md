# 개념 중심 기술 문서 작성 프롬프트

이 프롬프트를 사용하면 **구현 디테일이 아닌 기본 개념과 원리**를 중심으로 기술 문서를 작성할 수 있습니다.

---

## 📝 프롬프트 템플릿

```
주제: [기술 이름]

다음 구조로 기술 문서를 작성해줘:

# 목표
- 구현 방법이 아닌, **왜 이 기술이 필요한지**부터 시작
- **근본적인 문제**와 **해결 원리**에 집중
- 초보자도 이해할 수 있도록 **비유와 숫자**로 구체화
- 스캐폴딩 방식의 Q&A 대화 흐름

# Part 1: [이 기술이 뭐야?]
Q. [기존에 알고 있는 것]과 비교하면 뭐가 다를까?
→ 기본 정의, 왜 기존 방식으로 안 되는가?
→ 비유 추가 (일상적인 예시)

# Part 2: [왜 이게 문제야?]
Q. 구체적으로 얼마나 문제인가?
→ 숫자로 문제의 크기 표현 (예: X만 개, 초당 Y번)
→ "이게 [기술명]의 근본적 문제"
→ 비교 테이블 (문제 vs 해결방법)

# Part 3: [핵심 원리는?]
Q. 어떤 원리로 해결하는가?
→ 간단한 규칙/개념 설명
→ "이게 전부야" - 복잡하지 않음을 강조
→ 비유: [단순한 것 × 많은 수 = 복잡한 결과]

# Part 4: [최적화는?]
Q. 이것만으로 충분한가?
→ 추가로 필요한 최적화 기법
→ 얼마나 빨라지는가? (구체적 수치)
→ 비교 테이블 (방법 vs 성능)

# Part 5: [실제로는?]
Q. 실제 적용 시 고려사항은?
→ 예상치 못한 문제들
→ 해결 방법
→ 트레이드오프

# Part 6: [왜 중요해?]
Q. 이 기술이 어떤 영향을 주는가?
→ 실제 사례 (게임/서비스)
→ 이 기술이 없었다면?
→ 미래 발전 가능성

# 핵심 정리
- 6개 Part의 핵심을 한 줄씩 요약
- 구현 코드가 아닌 **개념**만 정리

---

# 작성 원칙
1. **"어떻게 구현"이 아닌 "왜 이렇게"에 집중**
2. **숫자로 구체화** (예: 48만 픽셀, 90% 절약)
3. **비유 2개 이상** 필수 (일상적인 것과 연결)
4. **문제 → 원리 → 최적화** 순서
5. **"이게 전부야"** - 핵심은 단순함을 강조
6. **비교 테이블** - 방법/성능/트레이드오프
7. **스캐폴딩 질문** - 독자가 스스로 깨닫도록
```

---

## 🎯 실전 사용 예시

### 예시 1: Redis가 MSSQL보다 빠른 이유

```
주제: Redis vs MSSQL 성능 차이

# Part 1: 캐시가 뭐야?
Q. DB에서 바로 읽으면 되는데 왜 캐시가 필요해?
→ DB는 디스크, 캐시는 메모리
→ 비유: 책장(DB) vs 책상 위(캐시)

# Part 2: 왜 디스크가 문제야?
Q. 디스크가 얼마나 느린데?
→ 디스크: 10ms, 메모리: 0.1ms = 100배 차이
→ 1000명 동시 접속 = 10초 대기 vs 0.1초

# Part 3: 핵심 원리는?
Q. Redis는 어떻게 빠를까?
→ In-Memory: 모든 데이터를 RAM에
→ Key-Value: 단순한 구조
→ "SELECT * FROM WHERE" 같은 복잡한 쿼리 불필요

# Part 4: 최적화는?
Q. 메모리에 다 올려도 될까?
→ 자주 쓰는 데이터만 (Hot Data)
→ TTL로 자동 삭제
→ 메모리 부족 시 LRU 정책

# Part 5: 실제로는?
Q. 그럼 DB는 필요 없어?
→ Redis = 휘발성 (재부팅 시 사라짐)
→ DB = 영구 저장
→ 역할 분담: Redis(읽기) + DB(쓰기/백업)

# Part 6: 왜 중요해?
Q. 실제로 얼마나 차이나?
→ 랭킹 조회: 0.8초 → 0.01초 (82배)
→ 동접자 증가해도 성능 유지
→ 게임 서버의 필수 요소

# 핵심 정리
- 캐시: 자주 쓰는 데이터를 메모리에
- 디스크 vs 메모리: 100배 속도 차이
- Redis: In-Memory + Key-Value
- Hot Data만 캐싱으로 메모리 절약
- DB와 역할 분담 (읽기/쓰기)
- 실측: 랭킹 조회 82배 빠름
```

---

## ✅ 체크리스트

문서 작성 후 확인:

- [ ] Part 1에서 기본 정의와 비교 (기존 vs 새로운)
- [ ] Part 2에서 문제를 **숫자로 구체화**
- [ ] Part 3에서 핵심 원리를 **3줄 이내**로 설명
- [ ] **비유가 2개 이상** 포함되어 있는가?
- [ ] **비교 테이블**이 있는가? (성능, 방법, 트레이드오프)
- [ ] "어떻게 구현"이 아닌 **"왜 이렇게"**에 집중했는가?
- [ ] 각 Part가 **Q&A 형식**으로 스캐폴딩되는가?
- [ ] 핵심 정리가 **개념**만 담고 있는가? (코드 X)

---

## 🎨 스타일 가이드

### 질문 만들기
```
❌ 나쁜 질문: "Redis의 특징은?"
✅ 좋은 질문: "DB에서 바로 읽으면 되는데 왜 캐시가 필요해?"

❌ 나쁜 질문: "청크 시스템이란?"
✅ 좋은 질문: "48만 개를 매번 다 확인해야 해?"
```

### 숫자 활용
```
❌ "매우 많은 연산이 필요하다"
✅ "800×600 = 48만 픽셀, 60fps = 초당 2,880만 연산"

❌ "Redis가 훨씬 빠르다"
✅ "0.8초 → 0.01초 (82배 빠름)"
```

### 비유 만들기
```
구조:
• [기존 개념] = [일상적인 것 A]
  → [특징 설명]
• [새로운 개념] = [일상적인 것 B]
  → [특징 설명]

예시:
• 강체 물리 = 레고 블록
  → 단단한 "물체"로 움직임
• 픽셀 물리 = 모래알
  → 한 알 한 알 개별적으로 떨어짐
```

### 강조 표현
```
• "이게 전부야" - 핵심이 단순함을 강조
• "이게 [기술명]의 근본적 문제" - 문제 정의
• "[결과]. 왜? [원리] 때문에" - 인과관계 명확히
• "그럼 [X]는 필요 없어? → [트레이드오프]" - 오해 방지
```

---

## 🔄 기존 문서 개선 시 사용법

1. **문제 정의부터 다시 시작**
   - "왜 이 기술이 필요한가?"
   - "어떤 문제를 해결하는가?"

2. **구현 디테일 제거**
   - 코드는 최소한만 (핵심 3줄 이내)
   - "이렇게 구현한다" → "왜 이렇게 하는가?"

3. **숫자 추가**
   - 모든 "많다/적다/빠르다/느리다"를 숫자로
   - 비교 전후 수치 명시

4. **비유 찾기**
   - 독자가 이미 아는 일상적인 것과 연결
   - 2개 이상 (개념 설명 1개 + 원리 설명 1개)

5. **스캐폴딩 질문 흐름**
   - 각 Part를 Q&A로 재구성
   - 독자가 "왜?"라고 물을 만한 곳에 답변

---

## 💡 핵심 철학

> **"무엇을 어떻게 구현했냐"가 아니라,**
> **"이 시스템의 근본 원리를 가장 초보적인 수준부터**
> **기술적 원리 이해에 입각한 스캐폴딩 질문으로"**

이 프롬프트의 목표는:
- 구현 튜토리얼이 아닌 **개념 이해**
- 코드가 아닌 **원리**
- 결과가 아닌 **과정**
- 답이 아닌 **질문**

---

**이 프롬프트를 AI에게 주제와 함께 입력하면, 위 스타일의 개념 중심 문서를 자동으로 생성할 수 있습니다.**
