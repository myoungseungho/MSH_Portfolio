# CLAUDE.md - MSH Portfolio 작성 가이드

이 문서는 Claude Code가 MSH Portfolio 프로젝트에서 학습 문서를 작성할 때 따라야 할 지침입니다.

---

## 📝 문서 작성 원칙

### 0. 제목은 후킹 포인트로

**제목은 독자의 호기심을 자극해야 합니다.**

```
❌ 나쁜 예: "웹의 발견 가능성 (Discoverability)"
❌ 나쁜 예: "AOI 개념 설명"

✅ 좋은 예: "왜 내 사이트는 Google에 안 뜰까?"
✅ 좋은 예: "MMO에서 1만 명을 어떻게 처리할까?"
✅ 좋은 예: "Redis가 MSSQL보다 82배 빠른 이유"
```

**후킹 패턴:**
- `왜 ~일까?` - 의문 유발
- `~하는 법` - 실용적 해결책
- `~가 ~보다 N배 빠른 이유` - 구체적 수치
- `~할 때 생기는 문제` - 공감 유발

---

### 1. 스캐폴딩 방식 (Scaffolding)

**독자가 스스로 깨달을 수 있도록 단계적 질문으로 유도합니다.**

```
❌ 나쁜 예: "AOI는 관심 영역입니다. 서버 부하를 줄이기 위해 사용합니다."

✅ 좋은 예:
Q. MMO에서 내가 서울에 있는데, 부산에 있는 유저 정보까지 받아야 해?
A. 필요 없지. 어차피 안 보이는데 왜 받아?
   그래서 나온 개념이 AOI (Area of Interest) = 관심 영역
```

**핵심: 답을 바로 주지 말고, 질문으로 생각을 유도한다.**

---

### 1-1. 초보자 중간비계 강제 규칙 (입도 높이기)

**전문용어를 알고 있다고 가정하지 않습니다.**

전문용어/약어가 처음 등장하면 반드시 아래 3단계를 즉시 붙입니다:

1. **한 줄 정의** - "이게 정확히 뭐야?"
2. **필요한 이유** - "왜 써야 해?"
3. **안 쓰면 생기는 문제** - "안 하면 뭐가 망가져?"

예시:

```html
<div class="chat">
    <div class="q">WIP Limit이 뭐야?</div>
    <div class="a">
        <p><strong>정의:</strong> 동시에 진행할 작업 개수의 상한선.</p>
        <p><strong>왜 필요:</strong> 여러 작업을 벌려놓고 전부 늦어지는 걸 막기 위해.</p>
        <p><strong>안 쓰면:</strong> "시작만 많은 팀"이 되고 완료율이 급락.</p>
    </div>
</div>
```

**강제 규칙:**
- 영어 약어는 첫 등장 시 반드시 풀어쓴다.
  - 예: `RACI (Responsible, Accountable, Consulted, Informed)`
- 풀어쓴 뒤 바로 한국어 의미를 붙인다.
- 한 문단에 새로운 전문용어를 2개 이상 넣지 않는다.
- 새 용어가 3개 이상 나오면, 문서 중간 또는 끝에 **용어 사전 블록(표/카드/목록 중 택1)**을 넣는다.

---

### 2. 대화형 Q&A 구조 (도입 권장)

문서 도입부는 **대화 형식**을 권장합니다:

```html
<div class="chat">
    <div class="q">왜 이게 필요해?</div>
    <div class="a">
        <p>설명...</p>
    </div>
</div>
```

**Q (질문)**: 독자가 자연스럽게 가질 만한 의문  
**A (답변)**: 간결하고 명확한 설명

단, 본문 전체를 반드시 Q&A로 고정할 필요는 없습니다.  
주제에 따라 표/리스트/도식/사례 설명을 자유롭게 섞어도 됩니다.

---

### 3. 시각 자료 (GIF/이미지) 선택 권장

개념 설명에 도움이 될 때 **단계별 시각 자료**를 포함합니다:

```
images/
├── step1.gif  - 문제 상황 시각화
├── step2.gif  - 첫 번째 해결 시도
├── step3.gif  - 개선된 방법
├── step4.gif  - 최적화
└── step5.gif  - 최종 결과
```

**GIF 제작 원칙:**
- 단순한 도형/색상으로 개념 표현
- 한 GIF당 하나의 개념만 설명
- 3~5초 내 핵심 전달

```html
<div class="visual-section">
    <h4>시뮬레이션: 문제 상황</h4>
    <img src="./images/step1.gif" alt="Step 1" class="step-gif">
    <p class="gif-caption">설명 캡션</p>
</div>
```

---

### 4. 하이라이트 시스템

중요도에 따라 다른 스타일 사용:

| 클래스 | 용도 | 색상 |
|--------|------|------|
| `.highlight` | 핵심 개념/용어 | 파란색 |
| `.key-point` | 핵심 포인트/결론 | 초록색 |
| `.warning` | 주의/경고/오해 | 빨간색 |

```html
<span class="highlight">Googlebot</span>  <!-- 핵심 개념 -->
<span class="key-point">정확해!</span>     <!-- 결론 -->
<span class="warning">불가능해.</span>     <!-- 주의 -->
```

---

### 5. 비유 활용

추상적 개념은 **일상적 비유**로 설명:

```html
<div class="analogy-box">
    <strong>🏪 비유: 가게 오픈</strong><br><br>
    당신이 서울 한복판에 가게를 열었어...
</div>
```

**좋은 비유의 조건:**
- 독자가 이미 아는 것과 연결
- 핵심 차이점을 명확히 드러냄
- 과도한 확장 금지 (비유는 비유일 뿐)

---

### 6. 비교 테이블

두 개 이상의 개념을 비교할 때 필요하면 사용:

```html
<table class="compare-table">
    <tr>
        <th>개념 A</th>
        <th>개념 B</th>
    </tr>
    <tr>
        <td>특징 1</td>
        <td>특징 1</td>
    </tr>
</table>
```

---

### 7. 핵심 정리 박스

문서 끝에 **핵심 요약** 필수:

```html
<div class="summary-box">
    <h3>📌 핵심 정리: 제목</h3>
    <ul>
        <li><strong>키워드</strong> - 한 줄 설명</li>
        ...
    </ul>
</div>
```

---

### 8. 코드 블록 줄바꿈

**코드 블록은 반드시 줄바꿈이 보존되어야 합니다.**

CSS에 `white-space: pre-wrap;` 필수:

```css
.code-block {
    /* ... 다른 스타일 ... */
    white-space: pre-wrap;  /* 필수! 줄바꿈 보존 */
}
```

**HTML 작성 시:**
```html
<!-- ✅ 좋은 예: 줄바꿈 그대로 작성 -->
<div class="code-block">
1. 첫 번째 단계
2. 두 번째 단계
3. 세 번째 단계
</div>

<!-- ❌ 나쁜 예: 한 줄로 작성 -->
<div class="code-block">1. 첫 번째 단계 2. 두 번째 단계 3. 세 번째 단계</div>
```

---

### 9. 델타 인지 최소화 규칙

**독자가 "갑자기 점프했다"는 느낌이 들면 실패입니다.**

- 설명 순서는 기본적으로 `문제 상황 → 용어 정의 → 원리 → 예시 → 예외`를 권장한다.
- 단, 주제 전달에 더 유리하면 순서를 바꿔도 된다.
- "Part 1/2/3" 같은 고정 파트 번호는 강제하지 않는다.
- 추상어를 쓰면 바로 수치/상황 예시를 붙인다.
- 전문용어 나열 문장을 금지한다. (용어만 쉼표로 나열 X)

---

### 10. 레이아웃 자유도 규칙

- 공통으로 맞출 것은 **큰 레이아웃(헤더/본문 카드/요약/내비게이션)**까지만 둔다.
- 표/리스트/도식/코드블록은 **주제 설명에 필요한 경우에만** 사용한다.
- 같은 카테고리 문서라도 섹션 개수, 섹션 제목, 블록 조합이 달라도 된다.
- "스타일 통일"보다 "이 주제를 가장 빠르게 이해시키는 구조"를 우선한다.

---

## 🎨 디자인 시스템

### 색상 팔레트

| 용도 | 색상 코드 |
|------|-----------|
| 배경 | `#fafafa` |
| 카드 배경 | `white` |
| 테두리 | `#e0e0e0` |
| 제목 (h2) | `#1a237e` |
| 본문 | `#333` |
| 보조 텍스트 | `#666` |

### 카테고리별 강조색

| 카테고리 | 색상 |
|----------|------|
| DB | `#4CAF50` (초록) |
| 멀티스레드 | `#FF9800` (주황) |
| 네트워크 | `#00BCD4` (청록) |
| 웹서버 | `#2196F3` (파랑) |
| GPU | `#9C27B0` (보라) |
| CS | `#607D8B` (회색) |
| 언어 | `#E91E63` (분홍) |
| AI | `#7C3AED` (보라) |

---

## 📁 파일 구조

새 주제 추가 시:

```
study/
└── [주제명]/
    ├── index.html      # 메인 문서
    └── images/         # 시각 자료 (선택)
        ├── step1.gif
        ├── step2.gif
        └── ...
```

---

## ✅ 체크리스트

새 문서 작성 시 확인:

- [ ] **제목이 후킹 포인트인가?** (왜~?, ~하는 법, 구체적 수치)
- [ ] 스캐폴딩 방식으로 질문 → 답변 구조인가?
- [ ] 독자가 "왜?"라고 물을 만한 곳에 설명이 있는가?
- [ ] **전문용어 첫 등장 시 3단계(정의/필요/미사용 시 문제)를 설명했는가?**
- [ ] **영어 약어를 풀어쓰고, 바로 한국어 의미를 붙였는가?**
- [ ] **새 용어 3개 이상일 때 용어 사전 블록(표/카드/목록)을 넣었는가?**
- [ ] 문서 구조(섹션 수/순서/블록)가 이 주제 설명에 최적화되어 있는가?
- [ ] 표/리스트/도식/코드블록을 필요한 만큼만 사용했는가?
- [ ] 시각 자료가 필요할 때만 쓰였고, 실제 이해를 돕는가?
- [ ] 비유가 적절한가?
- [ ] **코드 블록에 white-space: pre-wrap 적용했는가?**
- [ ] **코드 블록 내용이 줄바꿈되어 있는가?**
- [ ] 핵심 정리 박스가 있는가?
- [ ] 카테고리 페이지에 링크 추가했는가?
- [ ] 메인 index.html 카운트 업데이트했는가?

---

## 🔗 참고 문서

기존 문서 스타일 참고:
- `study/aoi/index.html` - 대화형 + GIF 예시
- `study/seamless-world/` - 단계별 문서 구조
- `study/discoverability/` - 비유 활용 예시

---

**Remember:** 독자는 처음 배우는 사람이다. 당연하게 생각하지 말고, 모든 것을 설명하라.
